# 敏感文件: API 密钥不进入镜像,运行时用 --env-file 传入
.env

# 运行数据: 挂载卷持久化,不需要打包进镜像
data
.hf_cache

# Node / Ink UI (Docker 只跑 CLI,不打包 UI 依赖)
ui/node_modules
ui/.build

# Python / Git / 缓存
.git
__pycache__
*.pyc

# 历史记录等 JSON 运行时数据
*.json
