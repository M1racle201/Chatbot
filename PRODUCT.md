# Product

## Register

product

## Users

VibeChatbot 面向在 Windows、PowerShell 和常见终端中工作的开发者。用户通过键盘完成对话、文件处理、普通 Agent 任务和 Agentic RAG 任务，需要持续看到上下文、执行进度、结果与错误，而不是在多个页面之间切换。

## Product Purpose

VibeChatbot 是基于 DeepSeek 的终端 AI 助手。它采用统一任务模式：简单工具任务自动走快速通道，其余任务进入“复写、执行、核查”闭环。成功的界面应让用户始终知道当前项目、模式、Agent 状态、最近任务和下一步可执行操作，同时不干扰长文本与代码结果的阅读。

## Brand Personality

克制、专注、技术化。界面应像一间安静的开发工作台：信息密度高但不拥挤，状态明确但不喧闹，强调色只用于当前选择和关键反馈。

## Anti-references

- 不做霓虹赛博朋克、紫色渐变或装饰性发光。
- 不做玻璃拟态、营销型数据看板或层层嵌套的圆角卡片。
- 不用颜色伪造能力，也不展示后端没有提供的文件差异、任务状态或持久化线程。
- 不为了模仿 Web 控件而破坏终端键盘工作流。

## Design Principles

1. 对话与任务结果是主角，导航和状态只提供必要上下文。
2. 每个颜色都有语义：琥珀表示当前选择，青绿表示 Agent 状态，红色只表示错误。
3. 终端尺寸是设计输入，宽屏充分分栏，窄屏保持输入与最新消息可见。
4. 界面只呈现真实数据；缺少结构化字段时使用诚实、通用的标签。
5. 所有现有命令、事件和流式行为保持兼容。

## Accessibility & Inclusion

正文与背景保持高对比度，颜色从不作为唯一状态信号，状态同时提供文字或符号。布局支持 80×24 的基础终端并适配更宽窗口；长文本正常换行。界面不使用装饰动画，流式输出光标保持轻量。
